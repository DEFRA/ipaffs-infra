---
# Retired: each fixed-environment pipeline owns its own schedule.
# Keep the YAML path valid for the old ADO definition until it is disabled.
trigger: none
pr: none

pool:
  name: DEFRA-COMMON-ubuntu2204-SSV5

steps:
  - checkout: none
  - bash: |
      echo "This wrapper is retired. Use QA - DEV or QA - TST."
      echo "See docs/qa-environment-pipelines.md for the cutover procedure."
    displayName: Retired nightly wrapper (no tests queued)
