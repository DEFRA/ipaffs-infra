---
trigger:
  branches:
    exclude:
      - '*'

pr:
  branches:
    exclude:
      - '*'

pool:
  name: DEFRA-COMMON-ubuntu2204-SSV5

parameters:
  - name: environmentName
    displayName: Environment
    type: string
    default: DEV
    values:
      - DEV
      - TST
      - PRE
      - PRD
  - name: establishPeering
    displayName: Establish Peering To Hub
    type: boolean
    default: false

variables:
  - group: Common
  - group: "${{ parameters.environmentName }}"
  - template: "vars/common.yaml"
  - template: "vars/${{ lower(parameters.environmentName) }}.yaml"
  - name: helmDryRun
    ${{ if eq(variables['Build.SourceBranch'], 'refs/heads/main') }}:
      value: ''
    ${{ else }}:
      value: '--dry-run'

resources:
  repositories:
    - repository: PipelineCommon
      name: DEFRA-DEVOPS-COMMON/Defra.Pipeline.Common
      type: git
      ref: feature/v6-optional-subscription-for-private-endpoint-records

stages:
  - template: stages/entra-groups.yaml
    parameters:
      environmentName: ${{ parameters.environmentName }}

  - template: stages/deploy-bicep-resource-group.yaml
    parameters:
      environmentName: ${{ parameters.environmentName }}

  - template: stages/deploy-bicep-network.yaml
    parameters:
      environmentName: ${{ parameters.environmentName }}

  - ${{ if parameters.establishPeering }}:
      - template: stages/establish-peering.yaml
        parameters:
          environmentName: ${{ parameters.environmentName }}

  - ${{ if or(eq(parameters.environmentName, 'DEV'), eq(parameters.environmentName, 'PRD')) }}:
      - template: stages/deploy-bicep-acr.yaml
        parameters:
          environmentName: ${{ parameters.environmentName }}

  - template: stages/deploy-bicep-classic.yaml
    parameters:
      environmentName: ${{ parameters.environmentName }}
      ${{ if eq(parameters.environmentName, 'DEV') }}:
        classicEnvironmentName: SND
      ${{ else }}:
        classicEnvironmentName: ${{ parameters.environmentName }}

  - template: stages/deploy-bicep-classic-network.yaml
    parameters:
      environmentName: ${{ parameters.environmentName }}
      ${{ if eq(parameters.environmentName, 'DEV') }}:
        classicEnvironmentName: SND
      ${{ else }}:
        classicEnvironmentName: ${{ parameters.environmentName }}

  - template: stages/deploy-bicep-infra.yaml
    parameters:
      environmentName: ${{ parameters.environmentName }}

  - template: stages/infra-permissions.yaml
    parameters:
      environmentName: ${{ parameters.environmentName }}

  - template: stages/private-endpoint-dns-records.yaml
    parameters:
      environmentName: ${{ parameters.environmentName }}

  - template: stages/import-acr-artifacts.yaml
    parameters:
      environmentName: ${{ parameters.environmentName }}

  - template: stages/install-helm-charts.yaml
    parameters:
      environmentName: ${{ parameters.environmentName }}

  - template: stages/deploy-bicep-privatelink.yaml
    parameters:
      environmentName: ${{ parameters.environmentName }}

# vim: set ts=2 sts=2 sw=2 et:
