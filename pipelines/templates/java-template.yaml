---
parameters:
  - name: environmentName
    displayName: Environment
    type: string
    default: DEV

  - name: serviceName
    type: string

  - name: dockerFile
    type: string
    default: Dockerfile

  - name: migrations
    type: boolean
    default: false

  - name: mvnGoals
    type: string
    default: verify jacoco:report package

  - name: helmfileAction
    type: string
    default: apply

  - name: updateManifest
    type: boolean
    default: true

  - name: manifestBranch
    type: string
    default: auto

  - name: manifestTrunkBranch
    type: string
    default: master

stages:
  - stage: DetectContainerBuild
    displayName: Detect Container Build Requirement
    jobs:
      - job: DetermineBuildMode
        displayName: Determine if container build is required
        steps:
          - checkout: self
            fetchDepth: 2
            path: s/self

          - task: Bash@3
            name: SetBuildMode
            displayName: Detect config-only values changes
            env:
              SOURCE_VERSION: $(Build.SourceVersion)
            inputs:
              targetType: inline
              workingDirectory: $(Pipeline.Workspace)/s/self
              script: |
                set -euo pipefail

                head_commit="${SOURCE_VERSION:-}"
                if [[ -z "${head_commit}" ]] || ! git cat-file -e "${head_commit}^{commit}" >/dev/null 2>&1; then
                  head_commit="$(git rev-parse HEAD)"
                fi

                if git rev-parse "${head_commit}^" >/dev/null 2>&1; then
                  base_commit="$(git rev-parse "${head_commit}^")"
                else
                  base_commit="$(git hash-object -t tree /dev/null)"
                fi

                mapfile -t changed_files < <(git diff --name-only "${base_commit}" "${head_commit}" --)

                echo "Comparing changes from ${base_commit} to ${head_commit}"

                if [[ ${#changed_files[@]} -eq 0 ]]; then
                  echo "No changed files detected. Container build and package stages will run."
                  echo "##vso[task.setvariable variable=skipContainerBuild;isOutput=true]false"
                  exit 0
                fi

                printf 'Changed files:\n'
                printf ' - %s\n' "${changed_files[@]}"

                config_values_only=true
                for changed_file in "${changed_files[@]}"; do
                  if [[ ! "${changed_file}" =~ ^(deployment|config)(/[^/]+)?/values\.ya?ml$ ]]; then
                    config_values_only=false
                    break
                  fi
                done

                if [[ "${config_values_only}" == "true" ]]; then
                  echo "Only deployment/config values.yaml files changed. Skipping container build and package stages."
                  echo "##vso[task.setvariable variable=skipContainerBuild;isOutput=true]true"
                else
                  echo "Detected non-values changes. Container build and package stages will run."
                  echo "##vso[task.setvariable variable=skipContainerBuild;isOutput=true]false"
                fi

  - stage: BuildService
    displayName: Build Service
    dependsOn: DetectContainerBuild
    condition: and(
      eq(dependencies.DetectContainerBuild.result, 'Succeeded'),
      ne(dependencies.DetectContainerBuild.outputs['DetermineBuildMode.SetBuildMode.skipContainerBuild'], 'true')
      )
    jobs:
      - template: /pipelines/jobs/build-java-project.yaml@ipaffs-infra
        parameters:
          mvnGoals:  "${{ parameters.mvnGoals }}"

  - stage: PackageService
    displayName: Package Service Image
    dependsOn:
      - DetectContainerBuild
      - BuildService
    condition: and(
      eq(dependencies.DetectContainerBuild.result, 'Succeeded'),
      ne(dependencies.DetectContainerBuild.outputs['DetermineBuildMode.SetBuildMode.skipContainerBuild'], 'true'),
      eq(dependencies.BuildService.result, 'Succeeded')
      )
    jobs:
      - template: /pipelines/jobs/package-java-project.yaml@ipaffs-infra
        parameters:
          migrations: ${{ parameters.migrations }}
          serviceName:  "${{ parameters.serviceName }}"
          dockerFile: "${{ parameters.dockerFile }}"

  - ${{ if eq(parameters.updateManifest, true) }}:
    - stage: UpdateManifest
      displayName: Update ipaffs-manifest
      dependsOn:
        - DetectContainerBuild
        - PackageService
      variables:
        - name: skipContainerBuild
          value: $[ stageDependencies.DetectContainerBuild.DetermineBuildMode.outputs['SetBuildMode.skipContainerBuild'] ]
      condition: and(
        eq(dependencies.DetectContainerBuild.result, 'Succeeded'),
        startsWith(variables['Build.SourceBranch'], 'refs/heads/'),
        or(
          eq(dependencies.DetectContainerBuild.outputs['DetermineBuildMode.SetBuildMode.skipContainerBuild'], 'true'),
          eq(dependencies.PackageService.result, 'Succeeded')
          )
        )
      jobs:
        - job: UpdateManifest
          displayName: Update service values in ipaffs-manifest
          steps:
            - checkout: self
              path: s/self

            - checkout: ipaffs-manifest
              persistCredentials: true
              path: s/ipaffs-manifest

            - checkout: ipaffs-infra
              path: s/ipaffs-infra

            - task: Bash@3
              displayName: Resolve and checkout manifest branch
              env:
                MANIFEST_BRANCH: "${{ parameters.manifestBranch }}"
                MANIFEST_TRUNK_BRANCH: "${{ parameters.manifestTrunkBranch }}"
                SOURCE_BRANCH: $(Build.SourceBranch)
              inputs:
                targetType: inline
                workingDirectory: $(Pipeline.Workspace)/s/ipaffs-manifest
                script: |
                  set -euo pipefail

                  source_branch="${SOURCE_BRANCH#refs/heads/}"
                  target_branch="${MANIFEST_BRANCH}"

                  if [[ "${target_branch}" == "auto" ]]; then
                    if [[ "${source_branch}" == "main" ]] || [[ "${source_branch}" == "master" ]]; then
                      target_branch="${MANIFEST_TRUNK_BRANCH}"
                    else
                      target_branch="${source_branch}"
                    fi
                  fi

                  if [[ -z "${target_branch}" ]]; then
                    echo "Target manifest branch cannot be empty" >&2
                    exit 1
                  fi

                  git fetch origin

                  if ! git ls-remote --exit-code --heads origin "${target_branch}" >/dev/null; then
                    if [[ "${target_branch}" == "${MANIFEST_TRUNK_BRANCH}" ]]; then
                      echo "Target manifest trunk branch '${target_branch}' does not exist" >&2
                      exit 1
                    fi

                    echo "Manifest branch '${target_branch}' does not exist. Skipping manifest update."
                    echo "##vso[task.setvariable variable=skipManifestUpdate]true"
                    exit 0
                  fi

                  git checkout -B "${target_branch}" "origin/${target_branch}"
                  echo "Using manifest branch: ${target_branch}"
                  echo "##vso[task.setvariable variable=targetManifestBranch]${target_branch}"
                  echo "##vso[task.setvariable variable=skipManifestUpdate]false"

            - task: Bash@3
              displayName: Update service values files
              condition: ne(variables['skipManifestUpdate'], 'true')
              env:
                SERVICE_NAME: "${{ parameters.serviceName }}"
                BUILD_NUMBER: $(Build.BuildNumber)
                ACR_LOGIN_SERVER: $(acrLoginServer)
                SKIP_CONTAINER_IMAGE_UPDATE: $(skipContainerBuild)
                MANIFEST_ROOT: "$(Pipeline.Workspace)/s/ipaffs-manifest"
                SERVICE_ROOT: "$(Pipeline.Workspace)/s/self"
              inputs:
                targetType: filePath
                filePath: $(Pipeline.Workspace)/s/ipaffs-infra/scripts/pipeline/update-manifest-service-values.sh
                workingDirectory: $(Pipeline.Workspace)/s/ipaffs-infra

            - task: Bash@3
              displayName: Commit and push manifest changes
              condition: ne(variables['skipManifestUpdate'], 'true')
              env:
                SERVICE_NAME: "${{ parameters.serviceName }}"
                BUILD_NUMBER: $(Build.BuildNumber)
                MANIFEST_BRANCH: "$(targetManifestBranch)"
              inputs:
                targetType: inline
                workingDirectory: $(Pipeline.Workspace)/s/ipaffs-manifest
                script: |
                  set -euo pipefail

                  git config user.name "IPAFFS CI Bot"
                  git config user.email "ipaffs-ci-bot@defra.gov.uk"

                  git add \
                    "services/${SERVICE_NAME}/base.yaml" \
                    "environments/dev/${SERVICE_NAME}.yaml" \
                    "environments/tst/${SERVICE_NAME}.yaml" \
                    "environments/pre/${SERVICE_NAME}.yaml" \
                    "environments/prd/${SERVICE_NAME}.yaml"

                  if git diff --cached --quiet; then
                    echo "No manifest changes for ${SERVICE_NAME}"
                    exit 0
                  fi

                  git commit -m "Update ${SERVICE_NAME} values for build ${BUILD_NUMBER}"

                  if ! git ls-remote --exit-code --heads origin "${MANIFEST_BRANCH}" >/dev/null; then
                    echo "Manifest branch '${MANIFEST_BRANCH}' no longer exists. Skipping push."
                    exit 0
                  fi

                  git pull --rebase origin "${MANIFEST_BRANCH}"
                  git push origin "HEAD:${MANIFEST_BRANCH}"

# vim: set ts=2 sts=2 sw=2 et:
